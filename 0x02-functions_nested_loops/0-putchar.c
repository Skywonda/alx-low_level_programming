#include "main.h"

/**
 * main - Prints _putchar
 * followed by a new line
 * Return: Returns 0 (Success)

int main(void)
{
	chat text[8] = "_putchar"
	int i;

	for (i = 0; i < 8; i++)
	{
		putchar(text[i]);
	}
	puchar('\n');
	return (0);
}
