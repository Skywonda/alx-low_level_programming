#include <stdio.h>
/*
 * main - return all alphabets in lowercase  except q and e
 * Return: always return 0 (sucess)
 */

int main(void)
{
	char a= 'a';
	while (a <= 'z')
		if (a != 'q' && a != 'e')
			putchar(a);
		c++;
	putchar('\n');
	return (0)
}
