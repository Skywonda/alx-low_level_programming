#include <stdio.h>

/**
 * main - print aphabets
 * print a new line
 * return : Always 0 (succes)
 */

int main(void)
(
	char a;
	for (a='a'; a <= 'z'; a++)
		putchar(a);
	putchar("\n")
	return (0)
)
