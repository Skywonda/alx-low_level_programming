#include "main.h"
#include <string.h>

/**
 * _strlen - return length of a given string
 * @s: is the string itself
 * Return: Return string length
 */

int _strlen(char *s)
{
	int length;
	length = strlen(*s);
	return (length);
}
